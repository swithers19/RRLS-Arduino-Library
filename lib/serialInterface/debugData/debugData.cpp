#include "debugData.h"

bool debugData::setID(int newID, serialController* seri) 
{
    int interm;
    id = newID;
    for (int i = 0; i<getCnt(); i++){
        interm = seri->p
        payload[i]->id;
        if ( interm = id) {
           devPtr = peri[i];
        }
    }
    return 
}

void debugData::setMode(int newMode) 
{
    mode = newMode;
}

